jQuery.sap.declare("com.nvidia.vebs.util.formatter");

function setText(oControl, bKey) {
		if(bKey){
			this.addStyleClass("zep_TableKey");
		}
}

com.nvidia.vebs.util.formatter = {
	someFucntion: function(bKey, sValue) {
		if(bKey){
			setText();
		 
		}
	}
};